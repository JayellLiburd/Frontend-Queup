import { createContext } from "react";

// Main User Info
export const usersContext = createContext<any>(null);

// Card Modal
export const modelContext = createContext<any>(null);

// Active.jsx & Edit
export const MyRoster = createContext<any>(null);

// Line Overview & Edit
export const Links = createContext<any>(null);

// RealTime Database NewListener
export const Updater = createContext<any>(null);